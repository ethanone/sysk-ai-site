# ✅ 项目检查清单

> 在部署新网站前，使用这个清单确保所有配置都已正确设置。

---

## 🎯 品牌标识检查

- [ ] Logo 文件已放置在 `public/` 目录
- [ ] `src/app/layout.tsx` 中的 `metadata.icons` 已更新
- [ ] `src/app/page.tsx` 中导航栏 Logo 已更新（第 87 行附近）
- [ ] `src/app/page.tsx` 中页脚 Logo 已更新（第 678 行附近）
- [ ] 浏览器标签栏图标显示正确（清除缓存后测试）

## 📝 网站信息检查

- [ ] `src/app/layout.tsx` - `metadata.title` 已修改
- [ ] `src/app/layout.tsx` - `metadata.description` 已修改
- [ ] `src/app/layout.tsx` - `metadata.keywords` 已修改
- [ ] `src/app/layout.tsx` - `openGraph.url` 已修改为正确的域名
- [ ] `package.json` - `name` 已修改
- [ ] `package.json` - `description` 已修改

## 📦 内容数据检查

- [ ] `src/data/companyData.json` 已完全更新
- [ ] JSON 格式验证通过（使用 JSONLint 或 VSCode）
- [ ] 公司信息已更新
- [ ] 关于我们已更新
- [ ] 核心优势已更新
- [ ] 服务领域已更新
- [ ] 案例研究已更新
- [ ] 团队介绍已更新
- [ ] 联系方式已更新

## 🖼️ 图片资源检查

- [ ] 所有案例图片已替换（`public/images/case-*.webp`）
- [ ] 所有团队头像已替换（`public/images/team/*.jpg`）
- [ ] 图片尺寸符合建议（参考 `public/images/README.md`）
- [ ] 图片文件大小已优化（< 300KB）
- [ ] 图片格式正确（WebP/JPG/PNG）

## 🎨 样式主题检查（可选）

- [ ] `src/app/globals.css` 中的颜色变量已按需修改
- [ ] 主色（`--primary`）已设置
- [ ] 次色（`--secondary`）已设置
- [ ] 点缀色（`--accent`）已设置
- [ ] 页面显示效果符合预期

## 🧪 功能测试

- [ ] 本地开发模式正常运行（`npm run dev`）
- [ ] 生产构建成功（`npm run build`）
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 警告
- [ ] 所有页面链接正常
- [ ] 响应式设计在移动端正常显示

## 🚀 部署前检查

- [ ] Git 仓库已初始化
- [ ] 所有文件已提交到 Git
- [ ] 已推送到 GitHub
- [ ] Vercel 项目已创建并连接
- [ ] 环境变量已配置（如有需要）
- [ ] 自定义域名已设置（如有需要）

## 🌐 上线后验证

- [ ] 生产网站可访问
- [ ] Favicon 在浏览器标签栏正确显示
- [ ] 页面加载速度正常
- [ ] SEO 标签显示正确（查看源代码）
- [ ] 社交媒体分享卡片正常（OpenGraph）
- [ ] 移动端显示正常
- [ ] 不同浏览器显示正常

## 📊 性能优化检查（可选）

- [ ] 使用 Lighthouse 测试性能得分
- [ ] 图片已优化（WebP 格式）
- [ ] 使用 CDN 加速（Vercel 自动提供）
- [ ] 缓存策略已配置（`next.config.ts`）

---

## 🎯 快速验证命令

```bash
# 1. 检查 TypeScript 类型
npm run lint

# 2. 验证 JSON 格式
node -e "require('./src/data/companyData.json')"

# 3. 生产构建测试
npm run build

# 4. 搜索是否还有旧项目名称
grep -r "旧项目名" src/

# 5. 检查图片引用
grep -r "/images/" src/
```

---

## 📝 注意事项

1. **Favicon 缓存问题**：浏览器会缓存 favicon，测试时使用无痕模式或清除缓存。

2. **JSON 格式**：确保使用标准双引号 `""`，不要用智能引号 `" "`。

3. **图片路径**：所有 public 目录下的文件，路径以 `/` 开头，如 `/uumi-logo.png`。

4. **Git 提交**：确保 `.gitignore` 正确配置，不要提交 `node_modules` 等。

5. **Vercel 部署**：首次部署后，GitHub 推送会自动触发重新部署。

---

**完成所有检查项后，您的网站就可以正式上线了！** 🚀
